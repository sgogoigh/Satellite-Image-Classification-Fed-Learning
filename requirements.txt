# fedsat requirements
#
# On Google Colab, torch + torchvision are PRE-INSTALLED with a matching CUDA build —
# do NOT reinstall them (that can break the GPU runtime). The list below is what the
# notebooks add on top. Install with:  pip install -r requirements.txt
#
# ---- Core (needed from P0/P1) ----
datasets>=2.19            # EuroSAT loader (reads labels from the dataset's own ClassLabel)
hf_transfer>=0.1          # resilient chunked HF downloads (avoids the ~64% parquet stall on Colab)
scikit-learn>=1.3         # metrics: macro-F1, per-class F1, confusion matrix, Cohen's kappa
torchmetrics>=1.3         # optional GPU metrics
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
pyyaml>=6.0
tqdm>=4.66
pillow>=10.0

# ---- Federated core (needed from P2 onward) ----
# flwr[simulation]>=1.13   # Flower + Ray Simulation Engine  (uncomment in the P2 notebook)
# flwr-datasets>=0.5       # optional: standard partitioners (we also ship our own in data.py)

# torch>=2.2               # provided by Colab; listed for local/CI installs only
# torchvision>=0.17        # provided by Colab; listed for local/CI installs only
